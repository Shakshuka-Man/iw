# Tooling for the JupyterLite site (`python site.py`). NOT needed to use `iw` -- the library's only
# dependency is PyYAML, which Pyodide ships, so it runs unmodified in the browser.
# Pinned here as well as in pyproject.toml: CI installs this file and never installs the iw package,
# so without this line PyYAML arrives only as a transitive dependency of jupyter-events.
pyyaml~=6.0
jupyterlite-core~=0.8.1
jupyterlite-pyodide-kernel~=0.8.1

# jupyterlite-core refuses to add custom contents to a site without this. Without it the build fails
# with "jupyter-server is not installed. You cannot add custom content to jupyterlite."
jupyter-server~=2.14
